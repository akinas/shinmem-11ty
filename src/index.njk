<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>ShinMem Publishing Company</title>
</head>
<body>
  <h1>Book List</h1>
  <ul>
    {% for book in books %}
    <li>
      <strong>{{ book.title }}</strong> ({{ book.title_en }})<br/>
      Author: {{ book.author }} | Year: {{ book.year }}
    </li>
    {% endfor %}
  </ul>
</body>
</html>
